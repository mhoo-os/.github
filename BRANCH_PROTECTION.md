# Branch protection recommendation

Organization-wide rulesets should be introduced only after ownership and exception handling are agreed.

For now, protect `main` with pull requests, one approval, resolved conversations, blocked force pushes, and blocked deletion. Do not require CI checks until each repository has a real, stable check.
